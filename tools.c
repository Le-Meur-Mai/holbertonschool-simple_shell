#include "main.h"

/**
 * string_lenght- calculate the lenght of a string.
 * @string: the string you want to know the lenght of
 * Return: the number of characters of the string
 */
int string_lenght(char *string)
{
	int i = 0;

	for (; string[i] != '\0'; i++)
	{
	}
	return (i);
}
